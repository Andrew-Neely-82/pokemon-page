import { TabPanel } from "@mui/lab";

const GameVersions = ({ style }) => {
  return (
    <TabPanel value="3" style={style}>
      All Game Versions
    </TabPanel>
  );
};
export default GameVersions;
